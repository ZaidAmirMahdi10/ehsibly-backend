{
  "devDependencies": {
    "prisma": "^5.7.1"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "node src/server.js",
    "build": "prisma generate && babel src -d dist",
    "prestart": "yarn build"
  }
}
