{
  "name": "ali-alwash-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "npx prisma migrate deploy",
    "prestart": "yarn prisma:generate",
    "prisma:generate": "prisma generate",
    "reset-db": "npx prisma migrate dev --name update_invoice_schema --create-only"
  },
  "dependencies": {
    "@prisma/client": "^5.7.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "prisma": "^5.7.1"
  }
}
